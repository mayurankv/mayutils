"""Test suite for ``mayutils``.

The layout mirrors ``src/mayutils/`` one-for-one — each source file has a
matching ``test_<name>.py`` at the same relative path. Module-level tests
for a subpackage's ``__init__`` (or cross-file tests within the same
folder) live in ``test_module.py``.
"""
