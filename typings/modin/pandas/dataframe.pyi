"""
This type stub file was generated by pyright.
"""

import datetime
import numpy as np
import pandas
from typing import Any, Hashable, IO, Iterable, Optional, Sequence, TYPE_CHECKING, Union
from pandas._libs import lib
from pandas._typing import CompressionOptions, FilePath, IndexLabel, Scalar, StorageOptions, WriteBuffer
from pandas.util._decorators import doc
from modin.core.storage_formats.pandas.query_compiler_caster import EXTENSION_DICT_TYPE
from modin.logging import disable_logging
from modin.utils import _inherit_docstrings, expanduser_path_arg
from .base import BasePandasDataset
from .series import Series
from .utils import GET_BACKEND_DOC, SET_BACKEND_DOC, _doc_binary_op
from typing_extensions import Self
from modin.core.storage_formats import BaseQueryCompiler

"""
This type stub file was generated by pyright.
"""
if TYPE_CHECKING:
    ...
@_inherit_docstrings(pandas.DataFrame, excluded=[pandas.DataFrame.__init__], apilink="pandas.DataFrame")
class DataFrame(BasePandasDataset):
    """
    Modin distributed representation of ``pandas.DataFrame``.

    Internally, the data can be divided into partitions along both columns and rows
    in order to parallelize computations and utilize the user's hardware as much as possible.

    Inherit common for ``DataFrame``-s and ``Series`` functionality from the
    `BasePandasDataset` class.

    Parameters
    ----------
    data : DataFrame, Series, pandas.DataFrame, ndarray, Iterable or dict, optional
        Dict can contain ``Series``, arrays, constants, dataclass or list-like objects.
        If data is a dict, column order follows insertion-order.
    index : Index or array-like, optional
        Index to use for resulting frame. Will default to ``RangeIndex`` if no
        indexing information part of input data and no index provided.
    columns : Index or array-like, optional
        Column labels to use for resulting frame. Will default to
        ``RangeIndex`` if no column labels are provided.
    dtype : str, np.dtype, or pandas.ExtensionDtype, optional
        Data type to force. Only a single dtype is allowed. If None, infer.
    copy : bool, default: False
        Copy data from inputs. Only affects ``pandas.DataFrame`` / 2d ndarray input.
    query_compiler : BaseQueryCompiler, optional
        A query compiler object to create the ``DataFrame`` from.

    Notes
    -----
    ``DataFrame`` can be created either from passed `data` or `query_compiler`. If both
    parameters are provided, data source will be prioritized in the next order:

    1) Modin ``DataFrame`` or ``Series`` passed with `data` parameter.
    2) Query compiler from the `query_compiler` parameter.
    3) Various pandas/NumPy/Python data structures passed with `data` parameter.

    The last option is less desirable since import of such data structures is very
    inefficient, please use previously created Modin structures from the fist two
    options or import data using highly efficient Modin IO tools (for example
    ``pd.read_csv``).
    """
    _pandas_class = pandas.DataFrame
    _extensions: EXTENSION_DICT_TYPE = ...
    def __init__(self, data=..., index=..., columns=..., dtype=..., copy=..., query_compiler: BaseQueryCompiler = ...) -> None:
        ...
    
    def __repr__(self) -> str:
        """
        Return a string representation for a particular ``DataFrame``.

        Returns
        -------
        str
        """
        ...
    
    columns: pandas.Index = ...
    @property
    def ndim(self) -> int:
        """
        Return the number of dimensions of the underlying data, by definition 2.
        """
        ...
    
    def drop_duplicates(self, subset=..., *, keep=..., inplace=..., ignore_index=...) -> Union[DataFrame, None]:
        """
        Return ``DataFrame`` with duplicate rows removed.
        """
        ...
    
    @property
    def dtypes(self) -> pandas.Series:
        """
        Return the dtypes in the ``DataFrame``.
        """
        ...
    
    def duplicated(self, subset=..., keep=...) -> Series:
        """
        Return boolean ``Series`` denoting duplicate rows.
        """
        ...
    
    @property
    def empty(self) -> bool:
        """
        Indicate whether ``DataFrame`` is empty.
        """
        ...
    
    @property
    def axes(self) -> list[pandas.Index]:
        """
        Return a list representing the axes of the ``DataFrame``.
        """
        ...
    
    @property
    def shape(self) -> tuple[int, int]:
        """
        Return a tuple representing the dimensionality of the ``DataFrame``.
        """
        ...
    
    def add_prefix(self, prefix, axis=...) -> DataFrame:
        """
        Prefix labels with string `prefix`.
        """
        ...
    
    def add_suffix(self, suffix, axis=...) -> DataFrame:
        """
        Suffix labels with string `suffix`.
        """
        ...
    
    def map(self, func, na_action: Optional[str] = ..., **kwargs) -> DataFrame:
        ...
    
    def applymap(self, func, na_action: Optional[str] = ..., **kwargs) -> DataFrame:
        ...
    
    def apply(self, func, axis=..., raw=..., result_type=..., args=..., by_row=..., engine=..., engine_kwargs=..., **kwargs) -> Union[DataFrame, Series]:
        """
        Apply a function along an axis of the ``DataFrame``.
        """
        ...
    
    def groupby(self, by=..., axis=..., level=..., as_index=..., sort=..., group_keys=..., observed=..., dropna: bool = ...):
        """
        Group ``DataFrame`` using a mapper or by a ``Series`` of columns.
        """
        ...
    
    def keys(self) -> pandas.Index:
        """
        Get columns of the ``DataFrame``.
        """
        ...
    
    def transpose(self, copy=..., *args) -> DataFrame:
        """
        Transpose index and columns.
        """
        ...
    
    @property
    def T(self) -> DataFrame:
        ...
    
    def add(self, other, axis=..., level=..., fill_value=...) -> DataFrame:
        """
        Get addition of ``DataFrame`` and `other`, element-wise (binary operator `add`).
        """
        ...
    
    def assign(self, **kwargs) -> DataFrame:
        """
        Assign new columns to a ``DataFrame``.
        """
        ...
    
    def boxplot(self, column=..., by=..., ax=..., fontsize=..., rot=..., grid=..., figsize=..., layout=..., return_type=..., backend=..., **kwargs):
        """
        Make a box plot from ``DataFrame`` columns.
        """
        ...
    
    def combine(self, other, func, fill_value=..., overwrite=...) -> DataFrame:
        """
        Perform column-wise combine with another ``DataFrame``.
        """
        ...
    
    def compare(self, other, align_axis=..., keep_shape: bool = ..., keep_equal: bool = ..., result_names=...) -> DataFrame:
        """
        Compare to another ``DataFrame`` and show the differences.
        """
        ...
    
    def corr(self, method=..., min_periods=..., numeric_only=...) -> DataFrame:
        """
        Compute pairwise correlation of columns, excluding NA/null values.
        """
        ...
    
    def corrwith(self, other, axis=..., drop=..., method=..., numeric_only=...) -> Series:
        """
        Compute pairwise correlation.
        """
        ...
    
    def cov(self, min_periods=..., ddof: Optional[int] = ..., numeric_only=...) -> DataFrame:
        """
        Compute pairwise covariance of columns, excluding NA/null values.
        """
        ...
    
    def dot(self, other) -> Union[DataFrame, Series]:
        """
        Compute the matrix multiplication between the ``DataFrame`` and `other`.
        """
        ...
    
    def eq(self, other, axis=..., level=...) -> DataFrame:
        """
        Perform equality comparison of ``DataFrame`` and `other` (binary operator `eq`).
        """
        ...
    
    def equals(self, other) -> bool:
        """
        Test whether two objects contain the same elements.
        """
        ...
    
    def eval(self, expr, inplace=..., **kwargs):
        """
        Evaluate a string describing operations on ``DataFrame`` columns.
        """
        ...
    
    def fillna(self, value=..., *, method=..., axis=..., inplace=..., limit=..., downcast=...) -> Union[DataFrame, None]:
        """
        Fill NA/NaN values using the specified method.
        """
        ...
    
    def floordiv(self, other, axis=..., level=..., fill_value=...) -> DataFrame:
        """
        Get integer division of ``DataFrame`` and `other`, element-wise (binary operator `floordiv`).
        """
        ...
    
    @classmethod
    def from_dict(cls, data, orient=..., dtype=..., columns=...) -> DataFrame:
        """
        Construct ``DataFrame`` from dict of array-like or dicts.
        """
        ...
    
    @classmethod
    def from_records(cls, data, index=..., exclude=..., columns=..., coerce_float=..., nrows=...) -> DataFrame:
        """
        Convert structured or record ndarray to ``DataFrame``.
        """
        ...
    
    def ge(self, other, axis=..., level=...) -> DataFrame:
        """
        Get greater than or equal comparison of ``DataFrame`` and `other`, element-wise (binary operator `ge`).
        """
        ...
    
    def gt(self, other, axis=..., level=...) -> DataFrame:
        """
        Get greater than comparison of ``DataFrame`` and `other`, element-wise (binary operator `ge`).
        """
        ...
    
    def hist(data, column: IndexLabel | None = ..., by=..., grid: bool = ..., xlabelsize: int | None = ..., xrot: float | None = ..., ylabelsize: int | None = ..., yrot: float | None = ..., ax=..., sharex: bool = ..., sharey: bool = ..., figsize: tuple[int, int] | None = ..., layout: tuple[int, int] | None = ..., bins: int | Sequence[int] = ..., backend: str | None = ..., legend: bool = ..., **kwargs):
        """
        Make a histogram of the ``DataFrame``.
        """
        ...
    
    def info(self, verbose: Optional[bool] = ..., buf: Optional[IO[str]] = ..., max_cols: Optional[int] = ..., memory_usage: Optional[Union[bool, str]] = ..., show_counts: Optional[bool] = ...) -> None:
        """
        Print a concise summary of the ``DataFrame``.
        """
        ...
    
    def insert(self, loc, column, value, allow_duplicates=...) -> None:
        """
        Insert column into ``DataFrame`` at specified location.
        """
        ...
    
    def isna(self) -> DataFrame:
        """
        Detect missing values.

        Returns
        -------
        DataFrame
            The result of detecting missing values.
        """
        ...
    
    def isnull(self) -> DataFrame:
        """
        Detect missing values.

        Returns
        -------
        DataFrame
            The result of detecting missing values.
        """
        ...
    
    def iterrows(self) -> Iterable[tuple[Hashable, Series]]:
        """
        Iterate over ``DataFrame`` rows as (index, ``Series``) pairs.
        """
        ...
    
    def items(self) -> Iterable[tuple[Hashable, Series]]:
        """
        Iterate over (column name, ``Series``) pairs.
        """
        ...
    
    def itertuples(self, index=..., name=...) -> Iterable[tuple[Any, ...]]:
        """
        Iterate over ``DataFrame`` rows as ``namedtuple``-s.
        """
        ...
    
    def join(self, other, on=..., how=..., lsuffix=..., rsuffix=..., sort=..., validate=...) -> DataFrame:
        """
        Join columns of another ``DataFrame``.
        """
        ...
    
    def isetitem(self, loc, value) -> None:
        ...
    
    def le(self, other, axis=..., level=...) -> DataFrame:
        """
        Get less than or equal comparison of ``DataFrame`` and `other`, element-wise (binary operator `le`).
        """
        ...
    
    def lt(self, other, axis=..., level=...) -> DataFrame:
        """
        Get less than comparison of ``DataFrame`` and `other`, element-wise (binary operator `le`).
        """
        ...
    
    def melt(self, id_vars=..., value_vars=..., var_name=..., value_name=..., col_level=..., ignore_index=...) -> DataFrame:
        """
        Unpivot a ``DataFrame`` from wide to long format, optionally leaving identifiers set.
        """
        ...
    
    def merge(self, right, how=..., on=..., left_on=..., right_on=..., left_index=..., right_index=..., sort=..., suffixes=..., copy=..., indicator=..., validate=...) -> DataFrame:
        """
        Merge ``DataFrame`` or named ``Series`` objects with a database-style join.
        """
        ...
    
    def mod(self, other, axis=..., level=..., fill_value=...) -> DataFrame:
        """
        Get modulo of ``DataFrame`` and `other`, element-wise (binary operator `mod`).
        """
        ...
    
    def mul(self, other, axis=..., level=..., fill_value=...) -> DataFrame:
        """
        Get multiplication of ``DataFrame`` and `other`, element-wise (binary operator `mul`).
        """
        ...
    
    multiply = ...
    def rmul(self, other, axis=..., level=..., fill_value=...) -> DataFrame:
        """
        Get multiplication of ``DataFrame`` and `other`, element-wise (binary operator `mul`).
        """
        ...
    
    def ne(self, other, axis=..., level=...) -> DataFrame:
        """
        Get not equal comparison of ``DataFrame`` and `other`, element-wise (binary operator `ne`).
        """
        ...
    
    def nlargest(self, n, columns, keep=...) -> DataFrame:
        """
        Return the first `n` rows ordered by `columns` in descending order.
        """
        ...
    
    def nsmallest(self, n, columns, keep=...) -> DataFrame:
        """
        Return the first `n` rows ordered by `columns` in ascending order.
        """
        ...
    
    def unstack(self, level=..., fill_value=..., sort=...) -> Union[DataFrame, Series]:
        """
        Pivot a level of the (necessarily hierarchical) index labels.
        """
        ...
    
    def pivot(self, *, columns, index=..., values=...) -> DataFrame:
        """
        Return reshaped ``DataFrame`` organized by given index / column values.
        """
        ...
    
    def pivot_table(self, values=..., index=..., columns=..., aggfunc=..., fill_value=..., margins=..., dropna=..., margins_name=..., observed=..., sort=...) -> DataFrame:
        """
        Create a spreadsheet-style pivot table as a ``DataFrame``.
        """
        ...
    
    @property
    def plot(self, x=..., y=..., kind=..., ax=..., subplots=..., sharex=..., sharey=..., layout=..., figsize=..., use_index=..., title=..., grid=..., legend=..., style=..., logx=..., logy=..., loglog=..., xticks=..., yticks=..., xlim=..., ylim=..., rot=..., fontsize=..., colormap=..., table=..., yerr=..., xerr=..., secondary_y=..., sort_columns=..., **kwargs):
        """
        Make plots of ``DataFrame``.
        """
        ...
    
    def pow(self, other, axis=..., level=..., fill_value=...) -> DataFrame:
        """
        Get exponential power of ``DataFrame`` and `other`, element-wise (binary operator `pow`).
        """
        ...
    
    def prod(self, axis=..., skipna=..., numeric_only=..., min_count=..., **kwargs):
        """
        Return the product of the values over the requested axis.
        """
        ...
    
    product = ...
    def quantile(self, q=..., axis=..., numeric_only=..., interpolation=..., method=...) -> Union[DataFrame, Series]:
        ...
    
    _AXIS_ORDERS = ...
    _get_index_resolvers = ...
    def query(self, expr, inplace=..., **kwargs) -> Union[DataFrame, None]:
        """
        Query the columns of a ``DataFrame`` with a boolean expression.
        """
        ...
    
    def rename(self, mapper=..., index=..., columns=..., axis=..., copy=..., inplace=..., level=..., errors=...) -> Union[DataFrame, None]:
        """
        Alter axes labels.
        """
        ...
    
    def reindex(self, labels=..., *, index=..., columns=..., axis=..., method=..., copy=..., level=..., fill_value=..., limit=..., tolerance=...) -> DataFrame:
        ...
    
    def replace(self, to_replace=..., value=..., *, inplace: bool = ..., limit=..., regex: bool = ..., method: str | lib.NoDefault = ...) -> Union[DataFrame, None]:
        """
        Replace values given in `to_replace` with `value`.
        """
        ...
    
    def rfloordiv(self, other, axis=..., level=..., fill_value=...) -> DataFrame:
        """
        Get integer division of ``DataFrame`` and `other`, element-wise (binary operator `rfloordiv`).
        """
        ...
    
    def radd(self, other, axis=..., level=..., fill_value=...) -> DataFrame:
        """
        Get addition of ``DataFrame`` and `other`, element-wise (binary operator `radd`).
        """
        ...
    
    def rmod(self, other, axis=..., level=..., fill_value=...) -> DataFrame:
        """
        Get modulo of ``DataFrame`` and `other`, element-wise (binary operator `rmod`).
        """
        ...
    
    def rpow(self, other, axis=..., level=..., fill_value=...) -> DataFrame:
        """
        Get exponential power of ``DataFrame`` and `other`, element-wise (binary operator `rpow`).
        """
        ...
    
    def rsub(self, other, axis=..., level=..., fill_value=...) -> DataFrame:
        """
        Get subtraction of ``DataFrame`` and `other`, element-wise (binary operator `rsub`).
        """
        ...
    
    def rtruediv(self, other, axis=..., level=..., fill_value=...) -> DataFrame:
        """
        Get floating division of ``DataFrame`` and `other`, element-wise (binary operator `rtruediv`).
        """
        ...
    
    rdiv = ...
    def select_dtypes(self, include=..., exclude=...) -> DataFrame:
        """
        Return a subset of the ``DataFrame``'s columns based on the column dtypes.
        """
        ...
    
    def set_index(self, keys, *, drop=..., append=..., inplace=..., verify_integrity=...) -> Union[DataFrame, None]:
        """
        Set the ``DataFrame`` index using existing columns.
        """
        ...
    
    sparse = ...
    def squeeze(self, axis=...) -> Union[DataFrame, Series, Scalar]:
        """
        Squeeze 1 dimensional axis objects into scalars.
        """
        ...
    
    def stack(self, level=..., dropna=..., sort=..., future_stack=...) -> Union[DataFrame, Series]:
        """
        Stack the prescribed level(s) from columns to index.
        """
        ...
    
    def sub(self, other, axis=..., level=..., fill_value=...) -> DataFrame:
        """
        Get subtraction of ``DataFrame`` and `other`, element-wise (binary operator `sub`).
        """
        ...
    
    subtract = ...
    def sum(self, axis=..., skipna=..., numeric_only=..., min_count=..., **kwargs) -> Series:
        ...
    
    @expanduser_path_arg("path")
    def to_feather(self, path, **kwargs) -> None:
        """
        Write a ``DataFrame`` to the binary Feather format.
        """
        ...
    
    def to_gbq(self, destination_table, project_id=..., chunksize=..., reauth=..., if_exists=..., auth_local_webserver=..., table_schema=..., location=..., progress_bar=..., credentials=...) -> None:
        """
        Write a ``DataFrame`` to a Google BigQuery table.
        """
        ...
    
    @expanduser_path_arg("path")
    def to_orc(self, path=..., *, engine=..., index=..., engine_kwargs=...) -> Union[bytes, None]:
        ...
    
    @expanduser_path_arg("buf")
    def to_html(self, buf=..., columns=..., col_space=..., header=..., index=..., na_rep=..., formatters=..., float_format=..., sparsify=..., index_names=..., justify=..., max_rows=..., max_cols=..., show_dimensions=..., decimal=..., bold_rows=..., classes=..., escape=..., notebook=..., border=..., table_id=..., render_links=..., encoding=...) -> Union[str, None]:
        """
        Render a ``DataFrame`` as an HTML table.
        """
        ...
    
    @expanduser_path_arg("path")
    def to_parquet(self, path=..., engine=..., compression=..., index=..., partition_cols=..., storage_options: StorageOptions = ..., **kwargs) -> Union[bytes, None]:
        ...
    
    def to_period(self, freq=..., axis=..., copy=...) -> DataFrame:
        """
        Convert ``DataFrame`` from ``DatetimeIndex`` to ``PeriodIndex``.
        """
        ...
    
    def to_records(self, index=..., column_dtypes=..., index_dtypes=...) -> np.rec.recarray:
        """
        Convert ``DataFrame`` to a NumPy record array.
        """
        ...
    
    @expanduser_path_arg("path")
    def to_stata(self, path: FilePath | WriteBuffer[bytes], *, convert_dates: dict[Hashable, str] | None = ..., write_index: bool = ..., byteorder: str | None = ..., time_stamp: datetime.datetime | None = ..., data_label: str | None = ..., variable_labels: dict[Hashable, str] | None = ..., version: int | None = ..., convert_strl: Sequence[Hashable] | None = ..., compression: CompressionOptions = ..., storage_options: StorageOptions = ..., value_labels: dict[Hashable, dict[float | int, str]] | None = ...) -> None:
        ...
    
    @expanduser_path_arg("path_or_buffer")
    def to_xml(self, path_or_buffer=..., index=..., root_name=..., row_name=..., na_rep=..., attr_cols=..., elem_cols=..., namespaces=..., prefix=..., encoding=..., xml_declaration=..., pretty_print=..., parser=..., stylesheet=..., compression=..., storage_options=...) -> Union[str, None]:
        ...
    
    def to_timestamp(self, freq=..., how=..., axis=..., copy=...) -> DataFrame:
        """
        Cast to DatetimeIndex of timestamps, at *beginning* of period.
        """
        ...
    
    def truediv(self, other, axis=..., level=..., fill_value=...) -> DataFrame:
        """
        Get floating division of ``DataFrame`` and `other`, element-wise (binary operator `truediv`).
        """
        ...
    
    divide = ...
    def update(self, other, join=..., overwrite=..., filter_func=..., errors=...) -> None:
        """
        Modify in place using non-NA values from another ``DataFrame``.
        """
        ...
    
    def where(self, cond, other=..., *, inplace=..., axis=..., level=...) -> Union[DataFrame, None]:
        """
        Replace values where the condition is False.
        """
        ...
    
    @disable_logging
    def __getattribute__(self, item: str) -> Any:
        """
        Return attribute from the `BasePandasDataset`.

        Parameters
        ----------
        item : str
            Item to get.

        Returns
        -------
        Any
        """
        ...
    
    @disable_logging
    def __getattr__(self, key) -> Any:
        """
        Return item identified by `key`.

        Parameters
        ----------
        key : hashable
            Key to get.

        Returns
        -------
        Any

        Notes
        -----
        First try to use `__getattribute__` method. If it fails
        try to get `key` from ``DataFrame`` fields.
        """
        ...
    
    def __setattr__(self, key, value) -> None:
        """
        Set attribute `value` identified by `key`.

        Parameters
        ----------
        key : hashable
            Key to set.
        value : Any
            Value to set.

        Returns
        -------
        None
        """
        ...
    
    def __setitem__(self, key, value) -> None:
        """
        Set attribute `value` identified by `key`.

        Parameters
        ----------
        key : Any
            Key to set.
        value : Any
            Value to set.

        Returns
        -------
        None
        """
        ...
    
    def __iter__(self) -> Iterable[Hashable]:
        """
        Iterate over info axis.

        Returns
        -------
        iterable
            Iterator of the columns names.
        """
        ...
    
    def __contains__(self, key) -> bool:
        """
        Check if `key` in the ``DataFrame.columns``.

        Parameters
        ----------
        key : hashable
            Key to check the presence in the columns.

        Returns
        -------
        bool
        """
        ...
    
    def __round__(self, decimals=...) -> DataFrame:
        """
        Round each value in a ``DataFrame`` to the given number of decimals.

        Parameters
        ----------
        decimals : int, default: 0
            Number of decimal places to round to.

        Returns
        -------
        DataFrame
        """
        ...
    
    def __delitem__(self, key) -> None:
        """
        Delete item identified by `key` label.

        Parameters
        ----------
        key : hashable
            Key to delete.
        """
        ...
    
    @_doc_binary_op(operation="integer division and modulo", bin_op="divmod", returns="tuple of two DataFrames")
    def __divmod__(self, right) -> tuple[DataFrame, DataFrame]:
        ...
    
    @_doc_binary_op(operation="integer division and modulo", bin_op="divmod", right="left", returns="tuple of two DataFrames")
    def __rdivmod__(self, left) -> tuple[DataFrame, DataFrame]:
        ...
    
    __add__ = ...
    __iadd__ = ...
    __radd__ = ...
    __mul__ = ...
    __imul__ = ...
    __rmul__ = ...
    __pow__ = ...
    __ipow__ = ...
    __rpow__ = ...
    __sub__ = ...
    __isub__ = ...
    __rsub__ = ...
    __floordiv__ = ...
    __ifloordiv__ = ...
    __rfloordiv__ = ...
    __truediv__ = ...
    __itruediv__ = ...
    __rtruediv__ = ...
    __mod__ = ...
    __imod__ = ...
    __rmod__ = ...
    __rdiv__ = ...
    def __dataframe__(self, nan_as_null: bool = ..., allow_copy: bool = ...):
        """
        Get a Modin DataFrame that implements the dataframe exchange protocol.

        See more about the protocol in https://data-apis.org/dataframe-protocol/latest/index.html.

        Parameters
        ----------
        nan_as_null : bool, default: False
            A keyword intended for the consumer to tell the producer
            to overwrite null values in the data with ``NaN`` (or ``NaT``).
            This currently has no effect; once support for nullable extension
            dtypes is added, this value should be propagated to columns.
        allow_copy : bool, default: True
            A keyword that defines whether or not the library is allowed
            to make a copy of the data. For example, copying data would be necessary
            if a library supports strided buffers, given that this protocol
            specifies contiguous buffers. Currently, if the flag is set to ``False``
            and a copy is needed, a ``RuntimeError`` will be raised.

        Returns
        -------
        ProtocolDataframe
            A dataframe object following the dataframe protocol specification.
        """
        ...
    
    def __dataframe_consortium_standard__(self, *, api_version: str | None = ...):
        """
        Provide entry point to the Consortium DataFrame Standard API.

        This is developed and maintained outside of Modin.
        Please report any issues to https://github.com/data-apis/dataframe-api-compat.
        """
        ...
    
    @property
    def attrs(self) -> dict:
        """
        Return dictionary of global attributes of this dataset.
        """
        ...
    
    @property
    def style(self):
        """
        Return a Styler object.
        """
        ...
    
    def reindex_like(self: DataFrame, other, method=..., copy: Optional[bool] = ..., limit=..., tolerance=...) -> DataFrame:
        ...
    
    def __reduce__(self):
        ...
    
    @doc(SET_BACKEND_DOC, class_name=__qualname__)
    def set_backend(self, backend: str, inplace: bool = ..., *, switch_operation: Optional[str] = ...) -> Optional[Self]:
        ...
    
    move_to = ...
    @doc(GET_BACKEND_DOC, class_name=__qualname__)
    @disable_logging
    def get_backend(self) -> str:
        ...
    
    @disable_logging
    def __delattr__(self, name: str) -> None:
        """
        Delete attribute `name`.

        Parameters
        ----------
        name : str
            Name of the attribute to delete.

        Returns
        -------
        None
        """
        ...
    


