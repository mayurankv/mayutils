"""
This type stub file was generated by pyright.
"""

import datetime
from fractions import Fraction
from io import BytesIO
from typing import Any, Dict, List, Literal, Optional, Union
from xlsxwriter.chart import Chart
from xlsxwriter.color import Color
from xlsxwriter.image import Image
from . import xmlwriter
from .format import Format
from .xmlwriter import XMLwriter

"""
This type stub file was generated by pyright.
"""
re_dynamic_function = ...
def convert_cell_args(method):
    """
    Decorator function to convert A1 notation in cell method calls
    to the default row/col notation.

    """
    ...

def convert_range_args(method):
    """
    Decorator function to convert A1 notation in range method calls
    to the default row/col notation.

    """
    ...

def convert_column_args(method):
    """
    Decorator function to convert A1 notation in columns method calls
    to the default row/col notation.

    """
    ...

CellBlankTuple = ...
CellErrorTuple = ...
CellNumberTuple = ...
CellStringTuple = ...
CellBooleanTuple = ...
CellFormulaTuple = ...
CellDatetimeTuple = ...
CellRichStringTuple = ...
CellArrayFormulaTuple = ...
class Worksheet(xmlwriter.XMLwriter):
    """
    A class for writing the Excel XLSX Worksheet file.

    """
    def __init__(self) -> None:
        """
        Constructor.

        """
        ...
    
    @convert_cell_args
    def write(self, row: int, col: int, *args) -> Union[Literal[0, -1], Any]:
        """
        Write data to a worksheet cell by calling the appropriate write_*()
        method based on the type of data being passed.

        Args:
            row:   The cell row (zero indexed).
            col:   The cell column (zero indexed).
            *args: Args to pass to sub functions.

        Returns:
             0:    Success.
            -1:    Row or column is out of worksheet bounds.
            other: Return value of called method.

        """
        ...
    
    @convert_cell_args
    def write_string(self, row: int, col: int, string: str, cell_format: Optional[Format] = ...) -> Literal[0, -1, -2]:
        """
        Write a string to a worksheet cell.

        Args:
            row:    The cell row (zero indexed).
            col:    The cell column (zero indexed).
            string: Cell data. Str.
            format: An optional cell Format object.

        Returns:
            0:  Success.
            -1: Row or column is out of worksheet bounds.
            -2: String truncated to 32k characters.

        """
        ...
    
    @convert_cell_args
    def write_number(self, row: int, col: int, number: Union[int, float, Fraction], cell_format: Optional[Format] = ...) -> Literal[0, -1]:
        """
        Write a number to a worksheet cell.

        Args:
            row:         The cell row (zero indexed).
            col:         The cell column (zero indexed).
            number:      Cell data. Int or float.
            cell_format: An optional cell Format object.

        Returns:
            0:  Success.
            -1: Row or column is out of worksheet bounds.

        """
        ...
    
    @convert_cell_args
    def write_blank(self, row: int, col: int, blank: Any, cell_format: Optional[Format] = ...):
        """
        Write a blank cell with formatting to a worksheet cell. The blank
        token is ignored and the format only is written to the cell.

        Args:
            row:         The cell row (zero indexed).
            col:         The cell column (zero indexed).
            blank:       Any value. It is ignored.
            cell_format: An optional cell Format object.

        Returns:
            0:  Success.
            -1: Row or column is out of worksheet bounds.

        """
        ...
    
    @convert_cell_args
    def write_formula(self, row: int, col: int, formula: str, cell_format: Optional[Format] = ..., value=...) -> Literal[0, -1, -2]:
        """
        Write a formula to a worksheet cell.

        Args:
            row:         The cell row (zero indexed).
            col:         The cell column (zero indexed).
            formula:     Cell formula.
            cell_format: An optional cell Format object.
            value:       An optional value for the formula. Default is 0.

        Returns:
            0:  Success.
            -1: Row or column is out of worksheet bounds.
            -2: Formula can't be None or empty.

        """
        ...
    
    @convert_range_args
    def write_array_formula(self, first_row: int, first_col: int, last_row: int, last_col: int, formula: str, cell_format: Optional[Format] = ..., value=...) -> Literal[0, -1]:
        """
        Write a formula to a worksheet cell/range.

        Args:
            first_row:    The first row of the cell range. (zero indexed).
            first_col:    The first column of the cell range.
            last_row:     The last row of the cell range. (zero indexed).
            last_col:     The last column of the cell range.
            formula:      Cell formula.
            cell_format:  An optional cell Format object.
            value:        An optional value for the formula. Default is 0.

        Returns:
            0:  Success.
            -1: Row or column is out of worksheet bounds.

        """
        ...
    
    @convert_range_args
    def write_dynamic_array_formula(self, first_row: int, first_col: int, last_row: int, last_col: int, formula: str, cell_format: Optional[Format] = ..., value=...) -> Literal[0, -1]:
        """
        Write a dynamic array formula to a worksheet cell/range.

        Args:
            first_row:    The first row of the cell range. (zero indexed).
            first_col:    The first column of the cell range.
            last_row:     The last row of the cell range. (zero indexed).
            last_col:     The last column of the cell range.
            formula:      Cell formula.
            cell_format:  An optional cell Format object.
            value:        An optional value for the formula. Default is 0.

        Returns:
            0:  Success.
            -1: Row or column is out of worksheet bounds.

        """
        ...
    
    @convert_cell_args
    def write_datetime(self, row: int, col: int, date: datetime.datetime, cell_format: Optional[Format] = ...) -> Literal[0, -1]:
        """
        Write a date or time to a worksheet cell.

        Args:
            row:         The cell row (zero indexed).
            col:         The cell column (zero indexed).
            date:        Date and/or time as a datetime object.
            cell_format: A cell Format object.

        Returns:
            0:  Success.
            -1: Row or column is out of worksheet bounds.

        """
        ...
    
    @convert_cell_args
    def write_boolean(self, row: int, col: int, boolean: bool, cell_format: Optional[Format] = ...):
        """
        Write a boolean value to a worksheet cell.

        Args:
            row:         The cell row (zero indexed).
            col:         The cell column (zero indexed).
            boolean:     Cell data. bool type.
            cell_format: An optional cell Format object.

        Returns:
            0:  Success.
            -1: Row or column is out of worksheet bounds.

        """
        ...
    
    @convert_cell_args
    def write_url(self, row: int, col: int, url: str, cell_format: Optional[Format] = ..., string: Optional[str] = ..., tip: Optional[str] = ...):
        """
        Write a hyperlink to a worksheet cell.

        Args:
            row:    The cell row (zero indexed).
            col:    The cell column (zero indexed).
            url:    Hyperlink url.
            format: An optional cell Format object.
            string: An optional display string for the hyperlink.
            tip:    An optional tooltip.
        Returns:
            0:  Success.
            -1: Row or column is out of worksheet bounds.
            -2: String longer than 32767 characters.
            -3: URL longer than Excel limit of 255 characters.
            -4: Exceeds Excel limit of 65,530 urls per worksheet.
        """
        ...
    
    @convert_cell_args
    def write_rich_string(self, row: int, col: int, *args: Union[str, Format]) -> Literal[0, -1, -2, -3, -4, -5]:
        """
        Write a "rich" string with multiple formats to a worksheet cell.

        Args:
            row:          The cell row (zero indexed).
            col:          The cell column (zero indexed).
            string_parts: String and format pairs.
            cell_format:  Optional Format object.

        Returns:
            0:  Success.
            -1: Row or column is out of worksheet bounds.
            -2: String truncated to 32k characters.
            -3: 2 consecutive formats used.
            -4: Empty string used.
            -5: Insufficient parameters.

        """
        ...
    
    def add_write_handler(self, user_type, user_function) -> None:
        """
        Add a callback function to the write() method to handle user defined
        types.

        Args:
            user_type:      The user type() to match on.
            user_function:  The user defined function to write the type data.
        Returns:
            Nothing.

        """
        ...
    
    @convert_cell_args
    def write_row(self, row: int, col: int, data, cell_format: Optional[Format] = ...) -> Union[Literal[0], Any]:
        """
        Write a row of data starting from (row, col).

        Args:
            row:    The cell row (zero indexed).
            col:    The cell column (zero indexed).
            data:   A list of tokens to be written with write().
            format: An optional cell Format object.
        Returns:
            0:  Success.
            other: Return value of write() method.

        """
        ...
    
    @convert_cell_args
    def write_column(self, row: int, col: int, data, cell_format: Optional[Format] = ...) -> Union[Literal[0], Any]:
        """
        Write a column of data starting from (row, col).

        Args:
            row:    The cell row (zero indexed).
            col:    The cell column (zero indexed).
            data:   A list of tokens to be written with write().
            format: An optional cell Format object.
        Returns:
            0:  Success.
            other: Return value of write() method.

        """
        ...
    
    @convert_cell_args
    def insert_image(self, row: int, col: int, source: Union[str, BytesIO, Image], options: Optional[Dict[str, Any]] = ...) -> Literal[0, -1]:
        """
        Insert an image with its top-left corner in a worksheet cell.

        Args:
            row:      The cell row (zero indexed).
            col:      The cell column (zero indexed).
            source:   Filename, BytesIO, or Image object.
            options:  Position, scale, url and data stream of the image.

        Returns:
            0:  Success.
            -1: Row or column is out of worksheet bounds.

        """
        ...
    
    @convert_cell_args
    def embed_image(self, row: int, col: int, source: Union[str, BytesIO, Image], options: Optional[Dict[str, Any]] = ...) -> Literal[0, -1]:
        """
        Embed an image in a worksheet cell.

        Args:
            row:      The cell row (zero indexed).
            col:      The cell column (zero indexed).
            source:   Filename, BytesIO, or Image object.
            options:  Url and data stream of the image.

        Returns:
            0:  Success.
            -1: Row or column is out of worksheet bounds.

        """
        ...
    
    @convert_cell_args
    def insert_textbox(self, row: int, col: int, text: str, options: Optional[Dict[str, Any]] = ...) -> Literal[0, -1]:
        """
        Insert an textbox with its top-left corner in a worksheet cell.

        Args:
            row:      The cell row (zero indexed).
            col:      The cell column (zero indexed).
            text:     The text for the textbox.
            options:  Textbox options.

        Returns:
            0:  Success.
            -1: Row or column is out of worksheet bounds.

        """
        ...
    
    @convert_cell_args
    def insert_chart(self, row: int, col: int, chart: Chart, options: Optional[Dict[str, Any]] = ...) -> Literal[0, -1, -2]:
        """
        Insert an chart with its top-left corner in a worksheet cell.

        Args:
            row:     The cell row (zero indexed).
            col:     The cell column (zero indexed).
            chart:   Chart object.
            options: Position and scale of the chart.

        Returns:
            0:  Success.
            -1: Row or column is out of worksheet bounds.

        """
        ...
    
    @convert_cell_args
    def write_comment(self, row: int, col: int, comment: str, options: Optional[Dict[str, Any]] = ...) -> Literal[0, -1, -2]:
        """
        Write a comment to a worksheet cell.

        Args:
            row:     The cell row (zero indexed).
            col:     The cell column (zero indexed).
            comment: Cell comment. Str.
            options: Comment formatting options.

        Returns:
            0:  Success.
            -1: Row or column is out of worksheet bounds.
            -2: String longer than 32k characters.

        """
        ...
    
    def show_comments(self) -> None:
        """
        Make any comments in the worksheet visible.

        Args:
            None.

        Returns:
            Nothing.

        """
        ...
    
    def set_background(self, source: Union[str, BytesIO, Image], is_byte_stream: bool = ...) -> Literal[0]:
        """
        Set a background image for a worksheet.

        Args:
            source:         Filename, BytesIO, or Image object.
            is_byte_stream: Deprecated. Use a BytesIO object instead.

        Returns:
            0:  Success.

        """
        ...
    
    def set_comments_author(self, author) -> None:
        """
        Set the default author of the cell comments.

        Args:
            author: Comment author name. String.

        Returns:
            Nothing.

        """
        ...
    
    def get_name(self):
        """
        Retrieve the worksheet name.

        Args:
            None.

        Returns:
            Nothing.

        """
        ...
    
    def activate(self) -> None:
        """
        Set this worksheet as the active worksheet, i.e. the worksheet that is
        displayed when the workbook is opened. Also set it as selected.

        Note: An active worksheet cannot be hidden.

        Args:
            None.

        Returns:
            Nothing.

        """
        ...
    
    def select(self) -> None:
        """
        Set current worksheet as a selected worksheet, i.e. the worksheet
        has its tab highlighted.

        Note: A selected worksheet cannot be hidden.

        Args:
            None.

        Returns:
            Nothing.

        """
        ...
    
    def hide(self) -> None:
        """
        Hide the current worksheet.

        Args:
            None.

        Returns:
            Nothing.

        """
        ...
    
    def very_hidden(self) -> None:
        """
        Hide the current worksheet. This can only be unhidden by VBA.

        Args:
            None.

        Returns:
            Nothing.

        """
        ...
    
    def set_first_sheet(self) -> None:
        """
        Set current worksheet as the first visible sheet. This is necessary
        when there are a large number of worksheets and the activated
        worksheet is not visible on the screen.

        Note: A selected worksheet cannot be hidden.

        Args:
            None.

        Returns:
            Nothing.

        """
        ...
    
    @convert_column_args
    def set_column(self, first_col: int, last_col: int, width: Optional[float] = ..., cell_format: Optional[Format] = ..., options: Optional[Dict[str, Any]] = ...) -> Literal[0, -1]:
        """
        Set the width, and other properties of a single column or a
        range of columns.

        Args:
            first_col:   First column (zero-indexed).
            last_col:    Last column (zero-indexed). Can be same as first_col.
            width:       Column width. (optional).
            cell_format: Column cell_format. (optional).
            options:     Dict of options such as hidden and level.

        Returns:
            0:  Success.
            -1: Column number is out of worksheet bounds.

        """
        ...
    
    @convert_column_args
    def set_column_pixels(self, first_col: int, last_col: int, width: Optional[float] = ..., cell_format: Optional[Format] = ..., options: Optional[Dict[str, Any]] = ...) -> Literal[0, -1]:
        """
        Set the width, and other properties of a single column or a
        range of columns, where column width is in pixels.

        Args:
            first_col:   First column (zero-indexed).
            last_col:    Last column (zero-indexed). Can be same as first_col.
            width:       Column width in pixels. (optional).
            cell_format: Column cell_format. (optional).
            options:     Dict of options such as hidden and level.

        Returns:
            0:  Success.
            -1: Column number is out of worksheet bounds.

        """
        ...
    
    def autofit(self, max_width: int = ...) -> None:
        """
        Simulate autofit based on the data, and datatypes in each column.

        Args:
            max_width (optional): max column width to autofit, in pixels.

        Returns:
            Nothing.

        """
        ...
    
    def set_row(self, row: int, height: Optional[float] = ..., cell_format: Optional[Format] = ..., options: Optional[Dict[str, Any]] = ...) -> Literal[0, -1]:
        """
        Set the width, and other properties of a row.

        Args:
            row:         Row number (zero-indexed).
            height:      Row height. (optional).
            cell_format: Row cell_format. (optional).
            options:     Dict of options such as hidden, level and collapsed.

        Returns:
            0:  Success.
            -1: Row number is out of worksheet bounds.

        """
        ...
    
    def set_row_pixels(self, row: int, height: Optional[float] = ..., cell_format: Optional[Format] = ..., options: Optional[Dict[str, Any]] = ...) -> Literal[0, -1]:
        """
        Set the width (in pixels), and other properties of a row.

        Args:
            row:         Row number (zero-indexed).
            height:      Row height in pixels. (optional).
            cell_format: Row cell_format. (optional).
            options:     Dict of options such as hidden, level and collapsed.

        Returns:
            0:  Success.
            -1: Row number is out of worksheet bounds.

        """
        ...
    
    def set_default_row(self, height: Optional[float] = ..., hide_unused_rows: bool = ...) -> None:
        """
        Set the default row properties.

        Args:
            height:           Default height. Optional, defaults to 15.
            hide_unused_rows: Hide unused rows. Optional, defaults to False.

        Returns:
            Nothing.

        """
        ...
    
    @convert_range_args
    def merge_range(self, first_row: int, first_col: int, last_row: int, last_col: int, data: Any, cell_format: Optional[Format] = ...) -> int:
        """
        Merge a range of cells.

        Args:
            first_row:    The first row of the cell range. (zero indexed).
            first_col:    The first column of the cell range.
            last_row:     The last row of the cell range. (zero indexed).
            last_col:     The last column of the cell range.
            data:         Cell data.
            cell_format:  Cell Format object.

        Returns:
             0:    Success.
            -1:    Row or column is out of worksheet bounds.
            other: Return value of write().

        """
        ...
    
    @convert_range_args
    def autofilter(self, first_row: int, first_col: int, last_row: int, last_col: int) -> None:
        """
        Set the autofilter area in the worksheet.

        Args:
            first_row:    The first row of the cell range. (zero indexed).
            first_col:    The first column of the cell range.
            last_row:     The last row of the cell range. (zero indexed).
            last_col:     The last column of the cell range.

        Returns:
             Nothing.

        """
        ...
    
    def filter_column(self, col: int, criteria: str) -> None:
        """
        Set the column filter criteria.

        Args:
            col:       Filter column (zero-indexed).
            criteria:  Filter criteria.

        Returns:
             Nothing.

        """
        ...
    
    def filter_column_list(self, col: int, filters: List[str]) -> None:
        """
        Set the column filter criteria in Excel 2007 list style.

        Args:
            col:      Filter column (zero-indexed).
            filters:  List of filter criteria to match.

        Returns:
             Nothing.

        """
        ...
    
    @convert_range_args
    def data_validation(self, first_row: int, first_col: int, last_row: int, last_col: int, options: Optional[Dict[str, Any]] = ...) -> Literal[0, -1, -2]:
        """
        Add a data validation to a worksheet.

        Args:
            first_row:    The first row of the cell range. (zero indexed).
            first_col:    The first column of the cell range.
            last_row:     The last row of the cell range. (zero indexed).
            last_col:     The last column of the cell range.
            options:      Data validation options.

        Returns:
            0:  Success.
            -1: Row or column is out of worksheet bounds.
            -2: Incorrect parameter or option.
        """
        ...
    
    @convert_range_args
    def conditional_format(self, first_row: int, first_col: int, last_row: int, last_col: int, options: Optional[Dict[str, Any]] = ...) -> Literal[0, -1, -2]:
        """
        Add a conditional format to a worksheet.

        Args:
            first_row:    The first row of the cell range. (zero indexed).
            first_col:    The first column of the cell range.
            last_row:     The last row of the cell range. (zero indexed).
            last_col:     The last column of the cell range.
            options:      Conditional format options.

        Returns:
            0:  Success.
            -1: Row or column is out of worksheet bounds.
            -2: Incorrect parameter or option.
        """
        ...
    
    @convert_range_args
    def add_table(self, first_row: int, first_col: int, last_row: int, last_col: int, options: Optional[Dict[str, Any]] = ...) -> Literal[0, -1, -2, -3]:
        """
        Add an Excel table to a worksheet.

        Args:
            first_row:    The first row of the cell range. (zero indexed).
            first_col:    The first column of the cell range.
            last_row:     The last row of the cell range. (zero indexed).
            last_col:     The last column of the cell range.
            options:      Table format options. (Optional)

        Returns:
            0:  Success.
            -1: Row or column is out of worksheet bounds.
            -2: Incorrect parameter or option.
            -3: Not supported in constant_memory mode.
        """
        ...
    
    @convert_cell_args
    def add_sparkline(self, row: int, col: int, options: Optional[Dict[str, Any]] = ...) -> Literal[0, -1, -2]:
        """
        Add sparklines to the worksheet.

        Args:
            row:     The cell row (zero indexed).
            col:     The cell column (zero indexed).
            options: Sparkline formatting options.

        Returns:
            0:  Success.
            -1: Row or column is out of worksheet bounds.
            -2: Incorrect parameter or option.

        """
        ...
    
    @convert_range_args
    def set_selection(self, first_row: int, first_col: int, last_row: int, last_col: int) -> None:
        """
        Set the selected cell or cells in a worksheet

        Args:
            first_row:    The first row of the cell range. (zero indexed).
            first_col:    The first column of the cell range.
            last_row:     The last row of the cell range. (zero indexed).
            last_col:     The last column of the cell range.

        Returns:
            0:  Nothing.
        """
        ...
    
    @convert_cell_args
    def set_top_left_cell(self, row: int = ..., col: int = ...) -> None:
        """
        Set the first visible cell at the top left of a worksheet.

        Args:
            row: The cell row (zero indexed).
            col: The cell column (zero indexed).

        Returns:
            0:  Nothing.
        """
        ...
    
    def outline_settings(self, visible: bool = ..., symbols_below: bool = ..., symbols_right: bool = ..., auto_style: bool = ...) -> None:
        """
        Control outline settings.

        Args:
            visible:       Outlines are visible. Optional, defaults to True.
            symbols_below: Show row outline symbols below the outline bar.
                           Optional, defaults to True.
            symbols_right: Show column outline symbols to the right of the
                           outline bar. Optional, defaults to True.
            auto_style:    Use Automatic style. Optional, defaults to False.

        Returns:
            0:  Nothing.
        """
        ...
    
    @convert_cell_args
    def freeze_panes(self, row: int, col: int, top_row: Optional[int] = ..., left_col: Optional[int] = ..., pane_type: int = ...) -> None:
        """
        Create worksheet panes and mark them as frozen.

        Args:
            row:      The cell row (zero indexed).
            col:      The cell column (zero indexed).
            top_row:  Topmost visible row in scrolling region of pane.
            left_col: Leftmost visible row in scrolling region of pane.

        Returns:
            0:  Nothing.

        """
        ...
    
    @convert_cell_args
    def split_panes(self, x: float, y: float, top_row: Optional[int] = ..., left_col: Optional[int] = ...) -> None:
        """
        Create worksheet panes and mark them as split.

        Args:
            x:        The position for the vertical split.
            y:        The position for the horizontal split.
            top_row:  Topmost visible row in scrolling region of pane.
            left_col: Leftmost visible row in scrolling region of pane.

        Returns:
            0:  Nothing.

        """
        ...
    
    def set_zoom(self, zoom: int = ...) -> None:
        """
        Set the worksheet zoom factor.

        Args:
            zoom: Scale factor: 10 <= zoom <= 400.

        Returns:
            Nothing.

        """
        ...
    
    def right_to_left(self) -> None:
        """
        Display the worksheet right to left for some versions of Excel.

        Args:
            None.

        Returns:
            Nothing.

        """
        ...
    
    def hide_zero(self) -> None:
        """
        Hide zero values in worksheet cells.

        Args:
            None.

        Returns:
            Nothing.

        """
        ...
    
    def set_tab_color(self, color: Union[str, Color]) -> None:
        """
        Set the color of the worksheet tab.

        Args:
            color: A #RGB color index.

        Returns:
            Nothing.

        """
        ...
    
    def protect(self, password: str = ..., options: Optional[Dict[str, Any]] = ...) -> None:
        """
        Set the password and protection options of the worksheet.

        Args:
            password: An optional password string.
            options:  A dictionary of worksheet objects to protect.

        Returns:
            Nothing.

        """
        ...
    
    def unprotect_range(self, cell_range: str, range_name: Optional[str] = ..., password: Optional[str] = ...) -> int:
        """
        Unprotect ranges within a protected worksheet.

        Args:
            cell_range: The cell or cell range to unprotect.
            range_name: An optional name for the range.
            password:   An optional password string. (undocumented)

        Returns:
            0:  Success.
            -1: Parameter error.

        """
        ...
    
    @convert_cell_args
    def insert_button(self, row: int, col: int, options: Optional[Dict[str, Any]] = ...) -> Literal[0, -1]:
        """
        Insert a button form object into the worksheet.

        Args:
            row:     The cell row (zero indexed).
            col:     The cell column (zero indexed).
            options: Button formatting options.

        Returns:
            0:  Success.
            -1: Row or column is out of worksheet bounds.

        """
        ...
    
    @convert_cell_args
    def insert_checkbox(self, row: int, col: int, boolean: bool, cell_format: Optional[Format] = ...):
        """
        Insert a boolean checkbox in a worksheet cell.

        Args:
            row:          The cell row (zero indexed).
            col:          The cell column (zero indexed).
            boolean:      The boolean value to display as a checkbox.
            cell_format:  Cell Format object.  (optional)

        Returns:
            0:  Success.
            -1: Row or column is out of worksheet bounds.

        """
        ...
    
    def set_landscape(self) -> None:
        """
        Set the page orientation as landscape.

        Args:
            None.

        Returns:
            Nothing.

        """
        ...
    
    def set_portrait(self) -> None:
        """
        Set the page orientation as portrait.

        Args:
            None.

        Returns:
            Nothing.

        """
        ...
    
    def set_page_view(self, view: Literal[0, 1, 2] = ...) -> None:
        """
        Set the page view mode.

        Args:
            0: Normal view mode
            1: Page view mode (the default)
            2: Page break view mode

        Returns:
            Nothing.

        """
        ...
    
    def set_pagebreak_view(self) -> None:
        """
        Set the page view mode.

        Args:
            None.

        Returns:
            Nothing.

        """
        ...
    
    def set_paper(self, paper_size: Union[Literal[1, 9], int]) -> None:
        """
        Set the paper type. US Letter = 1, A4 = 9.

        Args:
            paper_size: Paper index.

        Returns:
            Nothing.

        """
        ...
    
    def center_horizontally(self) -> None:
        """
        Center the page horizontally.

        Args:
            None.

        Returns:
            Nothing.

        """
        ...
    
    def center_vertically(self) -> None:
        """
        Center the page vertically.

        Args:
            None.

        Returns:
            Nothing.

        """
        ...
    
    def set_margins(self, left: float = ..., right: float = ..., top: float = ..., bottom: float = ...) -> None:
        """
        Set all the page margins in inches.

        Args:
            left:   Left margin.
            right:  Right margin.
            top:    Top margin.
            bottom: Bottom margin.

        Returns:
            Nothing.

        """
        ...
    
    def set_header(self, header: str = ..., options: Optional[Dict[str, Any]] = ..., margin=...) -> None:
        """
        Set the page header caption and optional margin.

        Args:
            header:  Header string.
            margin:  Header margin.
            options: Header options, mainly for images.

        Returns:
            Nothing.

        """
        ...
    
    def set_footer(self, footer: str = ..., options: Optional[Dict[str, Any]] = ..., margin=...) -> None:
        """
        Set the page footer caption and optional margin.

        Args:
            footer:  Footer string.
            margin:  Footer margin.
            options: Footer options, mainly for images.

        Returns:
            Nothing.

        """
        ...
    
    def repeat_rows(self, first_row: int, last_row: Optional[int] = ...) -> None:
        """
        Set the rows to repeat at the top of each printed page.

        Args:
            first_row: Start row for range.
            last_row: End row for range.

        Returns:
            Nothing.

        """
        ...
    
    @convert_column_args
    def repeat_columns(self, first_col: int, last_col: Optional[int] = ...) -> None:
        """
        Set the columns to repeat at the left hand side of each printed page.

        Args:
            first_col: Start column for range.
            last_col: End column for range.

        Returns:
            Nothing.

        """
        ...
    
    def hide_gridlines(self, option: Literal[0, 1, 2] = ...) -> None:
        """
        Set the option to hide gridlines on the screen and the printed page.

        Args:
            option:    0 : Don't hide gridlines
                       1 : Hide printed gridlines only
                       2 : Hide screen and printed gridlines

        Returns:
            Nothing.

        """
        ...
    
    def print_row_col_headers(self) -> None:
        """
        Set the option to print the row and column headers on the printed page.

        Args:
            None.

        Returns:
            Nothing.

        """
        ...
    
    def hide_row_col_headers(self) -> None:
        """
        Set the option to hide the row and column headers on the worksheet.

        Args:
            None.

        Returns:
            Nothing.

        """
        ...
    
    @convert_range_args
    def print_area(self, first_row: int, first_col: int, last_row: int, last_col: int) -> Literal[0, -1]:
        """
        Set the print area in the current worksheet.

        Args:
            first_row:    The first row of the cell range. (zero indexed).
            first_col:    The first column of the cell range.
            last_row:     The last row of the cell range. (zero indexed).
            last_col:     The last column of the cell range.

        Returns:
            0:  Success.
            -1: Row or column is out of worksheet bounds.

        """
        ...
    
    def print_across(self) -> None:
        """
        Set the order in which pages are printed.

        Args:
            None.

        Returns:
            Nothing.

        """
        ...
    
    def fit_to_pages(self, width: int, height: int) -> None:
        """
        Fit the printed area to a specific number of pages both vertically and
        horizontally.

        Args:
            width:  Number of pages horizontally.
            height: Number of pages vertically.

        Returns:
            Nothing.

        """
        ...
    
    def set_start_page(self, start_page: int) -> None:
        """
        Set the start page number when printing.

        Args:
            start_page: Start page number.

        Returns:
            Nothing.

        """
        ...
    
    def set_print_scale(self, scale: int) -> None:
        """
        Set the scale factor for the printed page.

        Args:
            scale: Print scale. 10 <= scale <= 400.

        Returns:
            Nothing.

        """
        ...
    
    def print_black_and_white(self) -> None:
        """
        Set the option to print the worksheet in black and white.

        Args:
            None.

        Returns:
            Nothing.

        """
        ...
    
    def set_h_pagebreaks(self, breaks: List[int]) -> None:
        """
        Set the horizontal page breaks on a worksheet.

        Args:
            breaks: List of rows where the page breaks should be added.

        Returns:
            Nothing.

        """
        ...
    
    def set_v_pagebreaks(self, breaks: List[int]) -> None:
        """
        Set the horizontal page breaks on a worksheet.

        Args:
            breaks: List of columns where the page breaks should be added.

        Returns:
            Nothing.

        """
        ...
    
    def set_vba_name(self, name: Optional[str] = ...) -> None:
        """
        Set the VBA name for the worksheet. By default this is the
        same as the sheet name: i.e., Sheet1 etc.

        Args:
            name: The VBA name for the worksheet.

        Returns:
            Nothing.

        """
        ...
    
    def ignore_errors(self, options: Optional[Dict[str, Any]] = ...) -> Literal[0, -1]:
        """
        Ignore various Excel errors/warnings in a worksheet for user defined
        ranges.

        Args:
            options: A dict of ignore errors keys with cell range values.

        Returns:
            0: Success.
           -1: Incorrect parameter or option.

        """
        ...
    


